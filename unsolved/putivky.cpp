// Problem No.6

#include <bits/stdc++.h>
using namespace std;

int maxSum(int days[], int prices[])
{
	int max_day[15];
	return 0;
};

short int n, days_left[15], pr_day[15];

int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	
// Input section
	cin >> n;
	for (int i=0; i<n; i++)
		cin >> days_left[i] >> pr_day[i];

// 
	
	return 0;

}
